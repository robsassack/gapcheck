{
  "private": true,
  "scripts": {
    "test": "node tests/run-scoring-tests.js",
    "typecheck": "tsc --project jsconfig.json && tsc --project tests/jsconfig.json"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "typescript": "^7.0.2"
  }
}
